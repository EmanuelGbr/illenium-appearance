// Entry point for the restored NUI build.
// The original React NUI runtime is kept in source control so future changes
// can be rebuilt instead of editing web/dist manually. The upstream source
// archive is not split into modules in this restored tree yet, but this file
// is the source-level entrypoint used by the build scripts.
import './original-nui.js';
